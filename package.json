{
  "name": "my-userscripts",
  "version": "1.0.0",
  "description": "多入口油猴脚本项目",
  "type": "module",
  "scripts": {
    "dev": "cross-env USERSCRIPT_NAME=imageDownloader vite build --watch",
    "dev:video": "cross-env USERSCRIPT_NAME=videoDownloader vite build --watch",
    "build:image": "cross-env USERSCRIPT_NAME=imageDownloader EMPTY_OUT_DIR=true vite build",
    "build:video": "cross-env USERSCRIPT_NAME=videoDownloader EMPTY_OUT_DIR=false vite build",
    "build": "npm run build:image && npm run build:video",
    "test:image": "node --test src/scripts/imageDownloader/tests/*.test.js",
    "preview": "npm run build"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "vite": "^5.4.21"
  },
  "dependencies": {
    "gifenc": "^1.0.3"
  }
}
